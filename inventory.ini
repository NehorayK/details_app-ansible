[details_servers]
appserver ansible_host=127.0.0.1 ansible_port=2222

[details_servers:vars]
ansible_user=root
ansible_password=root        # ← password you set in the Dockerfile
ansible_become=true           # elevate to root (needed for systemctl)
ansible_become_method=sudo
ansible_become_pass=root      # password used by sudo
ansible_python_interpreter=/usr/bin/python3